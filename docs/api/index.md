# API Reference

Auto-generated from C++ headers in `include/axiom/`.

| Module | Description |
|--------|-------------|
| {doc}`tensor` | Core `Tensor` class — creation, indexing, views, properties |
| {doc}`operations` | Element-wise, reduction, and matrix operations (`axiom::ops`) |
| {doc}`linalg` | Linear algebra — SVD, eigendecomposition, solve, etc. (`axiom::linalg`) |
| {doc}`fft` | Fast Fourier transforms (`axiom::fft`) |
| {doc}`einops` | Einstein notation operations (`axiom::einops`) |
| {doc}`io` | Serialization — FlatBuffers `.axfb` and NumPy `.npy` (`axiom::io`) |
| {doc}`dtypes` | `DType`, `Device` enums and type utilities |
| {doc}`random` | Random number generation (`axiom::RandomGenerator`) |
| {doc}`numeric` | Mathematical constants and special values (`axiom::numeric`) |
| {doc}`system` | System info, threading, and debug utilities |
| {doc}`errors` | Exception class hierarchy |

```{toctree}
:maxdepth: 1
:hidden:

tensor
operations
linalg
fft
einops
io
dtypes
random
numeric
system
errors
```
